/**
 * Gold Standard Trainer app — private staff catalog (Firebase Hosting).
 * Override with VITE_TRAINER_APP_URL in .env.local if the deploy URL changes.
 */
export const TRAINER_APP_URL =
  import.meta.env.VITE_TRAINER_APP_URL ||
  'https://gsdt-trainer-private.web.app/';
