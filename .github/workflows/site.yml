name: Public site build and deploy

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'src/**'
      - 'shared/**'
      - 'public/**'
      - 'scripts/**'
      - 'index.html'
      - 'vite.config.ts'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/site.yml'
  pull_request:
    paths:
      - 'src/**'
      - 'shared/**'
      - 'public/**'
      - 'scripts/**'
      - 'index.html'
      - 'vite.config.ts'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/site.yml'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    # Needed so VITE_BOOKING_API_URL (environment secret) is available for the production build.
    environment: github-pages
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps

      - name: Build and prerender static HTML
        env:
          # Same Apps Script /exec URL as trainer-app VITE_BOOKING_API_URL
          VITE_FORM_ENDPOINT: ${{ secrets.VITE_BOOKING_API_URL }}
        run: npm run build

      - name: Verify prerendered routes exist
        run: |
          test -f docs/index.html
          test -f docs/about/index.html
          test -f docs/contact/index.html
          test -f docs/book/index.html
          test ! -e docs/start
          test -f docs/guide/index.html
          test -f docs/exam/index.html
          test -f docs/intelligence/index.html
          test -f docs/dog-personality/index.html
          test -f docs/breed-finder/index.html
          test -f docs/equipment/index.html
          test -f docs/services/index.html
          test -f docs/services/puppy-training/index.html
          test -f docs/areas/index.html
          test -f docs/areas/takaka/index.html
          test -f docs/guide/foundation/index.html
          test -f docs/.nojekyll
          test -f docs/404.html
          test -f docs/robots.txt
          test -f docs/sitemap.xml
          test -f docs/CNAME
          grep -q 'goldstandarddogtraining.nz' docs/CNAME
          grep -q 'Sitemap: https://goldstandarddogtraining.nz/sitemap.xml' docs/robots.txt
          grep -q 'dog trainer Golden Bay' docs/robots.txt
          grep -q 'User-agent: GPTBot' docs/robots.txt
          grep -q 'User-agent: PerplexityBot' docs/robots.txt
          grep -q 'https://goldstandarddogtraining.nz/book' docs/sitemap.xml
          grep -q 'https://goldstandarddogtraining.nz/equipment' docs/sitemap.xml
          grep -q 'https://goldstandarddogtraining.nz/services/puppy-training' docs/sitemap.xml
          grep -q 'https://goldstandarddogtraining.nz/areas/takaka' docs/sitemap.xml
          ! grep -q 'https://goldstandarddogtraining.nz/start' docs/sitemap.xml
          test -f docs/BingSiteAuth.xml
          grep -q 'Dog Trainer Golden Bay' docs/index.html
          grep -q 'dog trainer Golden Bay' docs/index.html
          grep -q 'data-seo-ready="true"' docs/about/index.html
          grep -q 'Gentle Leader' docs/equipment/index.html
          grep -q 'missing-framework' docs/guide/foundation/index.html
          grep -q "You're not failing" docs/guide/foundation/index.html
          grep -q 'href="/book"' docs/index.html
          ! grep -q 'href="/start"' docs/index.html
          grep -q 'gsdt-site-jsonld' docs/index.html
          grep -q 'ProfessionalService' docs/index.html
          grep -q 'OfferCatalog' docs/index.html
          grep -q 'href="/services/puppy-training"' docs/index.html
          grep -q 'Puppies started' docs/services/puppy-training/index.html
          grep -q 'puppy training Golden Bay' docs/services/puppy-training/index.html
          grep -q 'Client dog training guide by Warwick Marshall' docs/guide/foundation/index.html
          grep -q '"@type":"Service"' docs/services/puppy-training/index.html
          grep -q 'SoftwareApplication' docs/exam/index.html
          grep -q 'Dog training in Takaka' docs/areas/takaka/index.html
          grep -q '<h1' docs/book/index.html
          grep -q 'Breed Analysis' docs/index.html
          grep -q 'Breed Analysis' docs/guide/index.html
          grep -q 'breed-rankings' docs/intelligence/index.html
          grep -q 'rel="canonical"' docs/dog-personality/index.html
          grep -q 'noindex' docs/404.html
          grep -q 'First-party patterns' docs/about/index.html

      - name: Upload Pages artifact
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        continue-on-error: true

      - name: Wait before Pages deploy retry
        if: steps.deployment.outcome == 'failure'
        run: sleep 45

      - name: Deploy to GitHub Pages (retry)
        if: steps.deployment.outcome == 'failure'
        id: deployment_retry
        uses: actions/deploy-pages@v4

      - name: Fail if Pages deploy did not succeed
        if: steps.deployment.outcome == 'failure' && steps.deployment_retry.outcome == 'failure'
        run: |
          echo "::error::GitHub Pages deploy failed after retry. Re-run this workflow from Actions, or check repo Settings → Pages → Source is GitHub Actions."
          exit 1
