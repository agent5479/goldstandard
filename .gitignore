node_modules/
*.tsbuildinfo
*.local
.DS_Store

# Trainer app — hero images synced from public/images at dev/build time
trainer-app/public/images/

# Trainer app — bordered metadata icons generated at dev/build time
trainer-app/public/favicon/

# Local reference copy only — not part of the published site
demosuite/

# Local archive — setup guides, testing notes, dev scripts (not on GitHub)
zz/
